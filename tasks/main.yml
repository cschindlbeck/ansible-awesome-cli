---
# tasks file for ansible-awesome-cli

- name: Touch a file, using symbolic modes to set the permissions (equivalent to 0644)
  ansible.builtin.file:
    path: /home/{{ ansible_env.USER }}/foo.conf
    state: touch
    mode: u=rw,g=r,o=r
